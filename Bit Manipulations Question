1. Check if ith bit is set or not:
bool check_bit(int N,int i){
 if (N&(1<<i)){
   return true;
   }
else {
    return false;
  }
}

2.Set the ith bit:
N=N&(1<<i);

3. Toggle the ith bit:
N=N^(1<<i);

4.Clear the ith bit:
N=N&~(1<<i);
or using 
N=N&^(1<<i);

5.Last set bit:
N&(N-1);

6.power of 2 :
A number is power of 2 if the binary representation has only one 1 .: method 1: N&N-1 and method 2: N&((1<<k)-1)
(implies only one set bit)
if (N&N-1==0){
   printf("Yes it is the Power of 2 ");
    }
else{
  printf("No it is not power of 2");
}

7.To Divide a Number by 2 without using the divsion operator: right shift operator
x=x>>1;

8.To Multiply a number by 2 without using the multiplication operator: left shift operator:
x=x<<1;

9.Swapping 2 numbers without using third variable:
a=a^b;
b=a^b;
a=a^b;

10.Count number of set bits in number n:
int cnt;
while(n>0){
if (n%2==1){
cnt=cnt+1;}
n=n>>1;
}

printf("%d",cnt);

11.Minimum bits flips to convert a Number(N) to other Number(M) : idea: check at each bit the number of places where the bits are opposite to each which means is 
opposite bits leads to high and same bits leads to low => XOR operation
int k= M^N;
int result=0;
while(K>0){
if(k%2==1){
result++;
}
k=k>>1;
}

12.Single Number -I : Given  a array with elements , where only one elements occurs once and other occurs twice: a^a=0 concept is used and 0^a=a
int size=sizeof(arr)/sizeof(arr[0]);
int single=0;
for (int i=0;i<size:i++){
single=single^arr[i];
}
printf("%d",single);
}

13.Single Number-2: Given a array with elements , where only one elements occurs once and other occurs thrice : 
use frequency hash map
int freq_arr[10]={0};
int size=sizeof(arr)/sizeof(arr[0]);
for(int i=0;i<size;i++){
freq_arr[arr[i]]++;
}
for (int i=0;i<10;i++){
    if (freq_arr[i]==1){
         printf("%d",i);
         break;
     }
}

14.XOR of numbers in the given Range: idea is based on the oberservation 
code::
int xor_grn(int n){
int ans;
if(n%4==0){
 ans=N;
}
else if (n%4==1){
  ans=1;
}
else if(n%4==2){
ans=n+1;
}
else if (n%4==3){
ans=0;
}
return ans;
}
